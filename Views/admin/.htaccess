<IfModule mod_rewrite.c>
  RewriteEngine On

  # Página de contato
  # RewriteRule ^contato/?$ /contato.php [NC,L]
  # Página de exibição de um produto
  # RewriteRule ^([a-z])/?$ AJEMS/views/admin/index.php?page=$1 [NC]

</IfModule>